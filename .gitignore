# Python
__pycache__/
*.py[cod]
*.egg-info/
.env
venv/
.venv/

# Claude internal directory
.claude/

# Node / frontend build output
node_modules/
project/frontend/dist/

# Large downloaded datasets (see README — download and place manually)
data/ctu13/
data/CICDDos2019/

# Local baseline captures + derived flow CSVs (machine-specific; see docs/LOCAL_BASELINE_DATASET_PLAN.md)
data/baseline_train/
data/local_baseline/

# Model backup (previous CICDDoS models also live in git history)
project/models_backup_cicddos/
project/models_backup_*/

# Raw Wireshark packet captures
wireshark_dataset/
*.pcap
*.pcapng

# Derived flow CSVs + evaluation reports (regenerate with flows_to_cicds.py /
# eval_captured_pcap.py; the ground-truth attack_timeline_*.csv files they are
# labelled against live in data/attack_timelines/ and ARE tracked)
data/captured_cicds/
project/models/captured_*.json
project/models/capture3_eval.json

# Runtime state (created automatically by the apps)
temp_uploads/
project/logs/
project/datasets/
project/reports/
research/results/
*.sqlite

project/models/aggregate/
